# test_geo

## How to run

1. git clone
1. cd to test_geo folder and run `npm i`
1. go to `test-issue` folder
1. in `test-issue` folder run `npm i`
1. open terminal for `test-issue` folder and run `npm run ng serve`
1. Open browser `localhost:4200`
